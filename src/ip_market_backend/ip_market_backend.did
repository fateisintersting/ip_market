type IPEntry = record {
  title: text;
  description: text;
  ip_type: text;
  metadata: text;
  image_url: opt text;
  additional_files: vec text;
};

service : {
  register_ip: (IPEntry) -> (text);
  get_my_ips: () -> (vec IPEntry);
}
